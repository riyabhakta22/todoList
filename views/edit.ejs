<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            font-family: "Montserrat", sans-serif;
            font-weight: 300;
            font-size: 15px;
            color: #f5f5f5;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
        }

        #todolist {
            margin-top: 60px;
            flex-basis: 450px;
            background-color: #1e272e;
            padding: 30px;
        }

        #todolist .task-head {
            display: flex;
            justify-content: space-between;
        }

        #todolist .task-head input {
            font-family: "Montserrat", sans-serif;
            font-size: 15px;
            flex-grow: 0.95;
            outline: none;
            border: none;
            color: #ff6f6f;
            border-bottom: 1px solid #ff6f6f;
            background: transparent;
            padding: 10px 10px 10px 0px;
        }

        #todolist .task-head input::placeholder {
            color: #ff6f6f;
            font-family: "Montserrat", sans-serif;
            font-size: 15px;
        }

        #todolist .task-head button {
            border: none;
            font-family: "Montserrat", sans-serif;
            background-color: #ff2b2b;
            color: #ffffff;
            padding: 10px 15px;
            font-weight: bold;
        }

        form#add-task {
            width: 100%;
        }

        form#add-task button {
            cursor: pointer;
        }
    </style>
</head>

<body>
    <div id="todolist">
        <form action="/editTask?Id=<%= Id %>" method="post" id="add-task" class="task-head">
            <input type="text" id="task" name="editTask" value="<%= data %>" required />
            <button>EDIT TASK</button>
        </form>
    </div>
</body>

</html>